// Stub for the `server-only` package under vitest. The real package is a
// build-time guard that errors if a server module is bundled for the client;
// it has no runtime behaviour, so an empty module is a faithful test stand-in.
export {};
